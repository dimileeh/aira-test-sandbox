# Aira Test Sandbox

Test repository for Aira AI agent task lifecycle testing.

## Purpose

This repo is used to verify the full agent task lifecycle:
1. Task assigned to AI agent
2. Agent clones repo, creates branch, implements changes
3. Agent opens PR with `[AIRA-TASK-KEY]` in title
4. PR merged → acceptance criteria evaluated → task closed
